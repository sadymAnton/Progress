
Процедура ОбработкаПроведения(Отказ, Режим)
	// регистр ДС_ПравилаПереносаСчетаЗатрат
	Движения.ДС_ПравилаПереносаСчетаЗатрат.Записывать = Истина;
	
	Для каждого СтрокаТЧ Из ЗатратныеСчета Цикл
		Движение = Движения.ДС_ПравилаПереносаСчетаЗатрат.Добавить();
		ЗаполнитьЗначенияСвойств(Движение, СтрокаТЧ);
		Движение.Период = Дата;
	КонецЦикла;
КонецПроцедуры //ОбработкаПроведения()
